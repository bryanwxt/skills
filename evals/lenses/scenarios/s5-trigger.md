You are choosing which skills to load. Read the available skills list at {{SKILL_LIST}} and the requests at {{FIXTURES}}/trigger-prompts.md. Read no other files. Decide based only on the skill descriptions.

Answer with ONLY a JSON object: {"P1": ["skill-name", ...], ..., "P10": [...]}, using the exact skill names from the list.
